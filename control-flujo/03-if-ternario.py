edad = 19

# El proximo codigo se conoce como operador terniario lo que hace es cambir la forma del if
# Donde le asignamos a una variable dos strings donde uno se ejecuto de primero y si no se cumple se ejecuta el ulrimo
# La fuente del operador ternario parte desde el centro donde se debe cumplir todo lo que la condicial if posea en su condicion
mensaje = "es mayor" if edad > 17 else "es menor"

# if edad > 17:
#     mensaje = "es mayor"
# else:
#     mensaje = "es menor"

print(mensaje)
